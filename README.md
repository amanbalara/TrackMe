# TrackMe

## Overview
TrackMe is a project that aims to help users track their activities and manage their time effectively.

## Setup Instructions
1. Clone the repository: `git clone https://github.com/amanbalara/TrackMe.git`
2. Navigate to the project directory: `cd TrackMe`
3. Install dependencies: `npm install`
4. Start the application: `npm start`
